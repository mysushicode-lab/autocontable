version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    volumes:
      - postgres-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=autocontable
      - POSTGRES_USER=autocontable
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-changeme}
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U autocontable"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      # Override .env with Docker-specific values
      - DATABASE_URL=postgresql://autocontable:${POSTGRES_PASSWORD:-changeme}@postgres:5432/autocontable
      - PYTHONPATH=/app
    volumes:
      - app-data:/app/data
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy

  scheduler:
    build: .
    restart: unless-stopped
    command: python -m src.scheduler.main
    env_file:
      - .env
    environment:
      # Override .env with Docker-specific values
      - DATABASE_URL=postgresql://autocontable:${POSTGRES_PASSWORD:-changeme}@postgres:5432/autocontable
      - PYTHONPATH=/app
    volumes:
      - app-data:/app/data
    depends_on:
      postgres:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        # Build-time: baked into client JS bundle (visible in browser)
        - NEXT_PUBLIC_API_URL=/api
        - NEXT_PUBLIC_SITE_URL=http://localhost:3001
    restart: unless-stopped
    environment:
      # Runtime: server-side only (used by next.config.js rewrites)
      - BACKEND_URL=http://backend:8000
    ports:
      - "3001:3000"
    depends_on:
      - backend

  backup:
    build: .
    restart: unless-stopped
    command: >
      sh -c "while true; do
        echo '[backup] Running daily backup...';
        /app/scripts/backup.sh;
        sleep 86400;
      done"
    environment:
      - DATA_DIR=/app/data
      - BACKUP_DIR=/app/backups
      - RETENTION_DAYS=30
    volumes:
      - app-data:/app/data
      - backup-data:/app/backups

volumes:
  app-data:
  backup-data:
  postgres-data:
